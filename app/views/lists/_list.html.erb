<%= link_to list_path(list), id: dom_id(list), data: { turbo_frame: "tasks", tabs_target: "tab", action: "click->tabs#toggle" }, class: "list-tab font-medium w-auto py-4 mr-8 border-b-2 #{ active_list&.id == list.id ? 'text-indigo-600 border-indigo-500 border-b-2' : 'text-gray-500 border-transparent hover:text-gray-600 hover:border-b-2 hover:border-gray-200' }" do %>
  <%= list.name %>
<% end %>
